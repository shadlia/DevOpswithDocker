```

the command i used to start the container with a bind mount is :

>docker run -v "%cd%/logs:/usr/src/app/text.log" devopsdockeruh/simple-web-service

Note : i used %cd% because im using windows as OS
```
